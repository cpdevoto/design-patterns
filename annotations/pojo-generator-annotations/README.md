# pojo-generator-annotations

**Owner(s):** Carlos Devoto

A library containing annotations that are used to generate Java POJO classes using an annotation processor.  This library should be used in conjunction with the ``pojo-generator-processor`` library, which contains additional instructions for setup. 
