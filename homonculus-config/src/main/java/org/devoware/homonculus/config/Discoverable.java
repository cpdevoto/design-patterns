package org.devoware.homonculus.config;

/**
 * A tag interface which allows Homonculus to load Jackson subtypes at runtime, which enables
 * polymorphic configurations.
 */
public interface Discoverable {
}
